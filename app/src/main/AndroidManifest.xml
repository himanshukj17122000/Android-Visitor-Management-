<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mockups">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".type"></activity>
        <activity android:name=".info" />
        <activity android:name=".finalpage" />
        <activity android:name=".idpage" />
        <activity android:name=".photo" />
        <activity android:name=".purpose" />
        <activity android:name=".bookingid" />
        <activity android:name=".secondpage" />
        <activity android:name=".phoonelogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>